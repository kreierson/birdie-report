---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('blog').catch(() => []);
const posts = allPosts
  .filter(post => post.data.category === 'opinion')
  .sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime());
---

<BaseLayout 
  title="Opinion & Hot Takes | The Birdie Report"
  description="Opinionated takes on golf culture, equipment, and everything in between."
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="text-center mb-12">
      <h1 class="text-4xl font-bold text-gray-900 mb-4">Opinion & Hot Takes</h1>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto">Opinionated takes on golf culture, equipment, and everything in between.</p>
    </div>

    {posts.length > 0 ? (
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {posts.map(post => (
          <article class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden hover:shadow-lg transition-shadow">
            <a href={`/blog/${post.slug}/`}>
              <img 
                src={post.data.featured_image} 
                alt={post.data.title}
                class="w-full h-48 object-cover hover:scale-105 transition-transform duration-300"
                loading="lazy"
              />
            </a>
            <div class="p-6">
              <div class="flex items-center space-x-2 text-sm text-gray-500 mb-2">
                <span class="px-2 py-1 bg-red-100 text-red-700 rounded text-xs font-medium">OPINION</span>
                <time>{new Date(post.data.date).toLocaleDateString()}</time>
              </div>
              <h3 class="text-xl font-semibold text-gray-900 mb-3 line-clamp-2">
                <a href={`/blog/${post.slug}/`} class="hover:text-green-700 transition-colors">
                  {post.data.title}
                </a>
              </h3>
              <p class="text-gray-600 text-sm line-clamp-3">{post.data.description}</p>
            </div>
          </article>
        ))}
      </div>
    ) : (
      <p class="text-center text-gray-500 text-lg">Content coming soon.</p>
    )}
  </div>
</BaseLayout>
